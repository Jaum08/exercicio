#include <stdio.h>
int main(){
    int n,soma;
    printf("Dgite um numero positivo: ", n);
    scanf("%d", &n);

    for(int i=1;i<=n;i++){
        soma+=i
    }
    printf("A soma de todos os numero e:",n, soma);

    return 0;
}
